import { EventParams } from "./eventParams";

export class Parts {
    rideCym?: EventParams[];
    hatChick?: EventParams[];
    crossStick?: EventParams[];
    highTom?: EventParams[];
    snare?: EventParams[];
    kick?: EventParams[];
}